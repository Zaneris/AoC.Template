using AdventOfCodeSupport;

var solutions = new AdventSolutions();
var today = solutions.GetMostRecentDay();
// var lastYearDay = solutions.GetDay(2023, 1);

// Download today's input (needs session cookie set in user secrets)
// await today.DownloadInputAsync();

today.Part1(); // Run part 1 and print answer to console
// today.Part2() // Run part 2 and print answer to console

// Submit answers from code (needs session cookie set in user secrets)
// await today.SubmitPart1Async();
// await today.SubmitPart2Async();
